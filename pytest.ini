[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    spark: needs a working SparkSession (slow, starts a JVM)
    mongo: needs a running MongoDB
    stream: needs the full streaming stack
asyncio_default_fixture_loop_scope = function
